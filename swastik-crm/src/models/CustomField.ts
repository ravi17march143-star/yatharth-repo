import mongoose, { Schema, Document } from 'mongoose';
export interface ICustomField extends Document { fieldto: string; name: string; slug: string; required: number; type: string; options: string[]; display_order: number; active: number; show_on_pdf: number; only_admin: number; show_on_table: number; show_on_ticket_form: number; }
const CustomFieldSchema = new Schema<ICustomField>({ fieldto: { type: String, required: true }, name: { type: String, required: true }, slug: { type: String }, required: { type: Number, default: 0 }, type: { type: String, default: 'input' }, options: [{ type: String }], display_order: { type: Number, default: 0 }, active: { type: Number, default: 1 }, show_on_pdf: { type: Number, default: 0 }, only_admin: { type: Number, default: 0 }, show_on_table: { type: Number, default: 0 }, show_on_ticket_form: { type: Number, default: 0 } }, { timestamps: true });
export default mongoose.models.CustomField || mongoose.model<ICustomField>('CustomField', CustomFieldSchema);
